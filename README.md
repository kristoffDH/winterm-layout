window terminal을 이용한 terminal layout 설정 스크립트
powershell은 window 7을 사용 

각 layout 설정은 layout 폴더 안의 layouts 폴더 안에 json 파일로 위치
layout-이름.json 포맷으로 지정

실행 예
layout cds  # layout-cds.json 파일이 있다는 가정

layout cds -ReuseWindow # 현재 윈도우에서 스크립트 실행